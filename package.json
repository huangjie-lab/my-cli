{
  "name": "mycli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "mycli": "bin/cli.js"
  },
  "scripts": {
    "test": "node ./test/inquirer.js",
    "commitlint":"commitlint --config commitlint.config.js -e -V || (node ./scripts/commit-info.js && exit 8)",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.0",
    "download-git-repo": "^3.0.2",
    "husky": "^9.1.7",
    "inquirer": "^8.2.6",
    "lint-staged": "^15.2.10"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^8.0.0"
  }
}
